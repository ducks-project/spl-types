language: php
dist: trusty
php:
#  - 5.4
#  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3

#matrix:
#  include:
#    - php: 5.3
#      dist: precise

before_script:
  - composer install --no-interaction --optimize-autoloader
script:
  - mkdir -p build/logs
  - composer unittest
  - composer atoumtest

after_success:
  - travis_retry php vendor/bin/php-coveralls
